diff --git a/node_modules/a2a-codex/dist/cli.js b/node_modules/a2a-codex/dist/cli.js
index 5355292..fc7c945 100644
--- a/node_modules/a2a-codex/dist/cli.js
+++ b/node_modules/a2a-codex/dist/cli.js
@@ -162,7 +162,9 @@ async function main() {
     const shutdown = async (signal) => {
         log.info(`${signal} received, shutting down...`);
         await handle.shutdown();
-        process.exit(0);
+        // REQ-v0.2.1-2026-09-14-02: let the event loop drain instead of process.exit(0)
+        // (avoids UV_HANDLE_CLOSING assertion while stdout/handles are still closing).
+        process.exitCode = 0;
     };
     process.on("SIGINT", () => void shutdown("SIGINT"));
     process.on("SIGTERM", () => void shutdown("SIGTERM"));
@@ -170,6 +172,8 @@ async function main() {
 import { Logger } from "./utils/logger.js";
 main().catch((err) => {
     log.error("Fatal error", { error: err.message, stack: err.stack });
-    process.exit(1);
+    // REQ-v0.2.1-2026-09-14-02: set exitCode and let the loop drain instead of
+    // process.exit(1), which aborts with UV_HANDLE_CLOSING on Windows.
+    process.exitCode = 1;
 });
 //# sourceMappingURL=cli.js.map
\ No newline at end of file
diff --git a/node_modules/a2a-codex/dist/codex/executor.js b/node_modules/a2a-codex/dist/codex/executor.js
index c5706de..0f3ab74 100644
--- a/node_modules/a2a-codex/dist/codex/executor.js
+++ b/node_modules/a2a-codex/dist/codex/executor.js
@@ -16,9 +16,20 @@ import { EventMapper, sanitizeMessage } from "./event-mapper.js";
 import { validateMcpServers, toCodexMcpEntry } from "./mcp-adapter.js";
 import { CODEX_BACKEND_PATHS } from "./backend-paths.js";
 import { extractUserText } from "./prompt-builder.js";
-import { resolveTransport, AgentEventEmitter, materializeMemory, bootstrapSubAgents, publishTask, publishStatus, publishFinalArtifact, publishStreamingChunk, publishLastChunkMarker, } from "@a2a-wrapper/core";
+import { resolveTransport, AgentEventEmitter, materializeMemory, bootstrapSubAgents, publishTask, publishStatus, publishFinalArtifact, publishStreamingChunk, publishLastChunkMarker, resolveOutcome, } from "@a2a-wrapper/core";
 import { logger } from "../utils/logger.js";
 const log = logger.child("executor");
+// v0.1.2 统一任务执行日志（[a2a-task]）：直写 stdout，不受 logging.level 影响，供 Launcher 观测开始/结束/耗时。
+const __a2aTaskLog = {
+    start(taskId, contextId, promptLen) {
+        console.log(`[a2a-task] start taskId=${taskId} contextId=${contextId} promptLen=${promptLen}`);
+    },
+    end(taskId, contextId, state, responseChars, startedAt, error) {
+        const durationMs = Date.now() - startedAt;
+        const suffix = error ? ` error=${JSON.stringify(String(error).slice(0, 300))}` : "";
+        console.log(`[a2a-task] end taskId=${taskId} contextId=${contextId} state=${state} responseChars=${responseChars} durationMs=${durationMs}${suffix}`);
+    },
+};
 export class CodexExecutor {
     config;
     client = null;
@@ -90,6 +101,7 @@ export class CodexExecutor {
     // ── Task Execution ───────────────────────────────────────────────────────
     async execute(ctx, bus) {
         const { taskId, contextId, userMessage, task } = ctx;
+        let __taskT0 = null;
         await this.initialize();
         const agentId = this.config.agentCard.name.toLowerCase().replace(/\s+/g, "-");
         const transport = resolveTransport(this.config.events, bus, taskId, contextId, this.customTransport);
@@ -111,11 +123,34 @@ export class CodexExecutor {
             const session = this.sessionManager.getOrCreate(contextId, () => this.client.startThread(threadOpts));
             // Extract user prompt
             const promptText = extractUserText(userMessage);
-            log.info("Executing task", { taskId, contextId, promptLen: promptText.length });
+            __taskT0 = Date.now();
+            __a2aTaskLog.start(taskId, contextId, promptText.length);
             // Serialize execution — queue behind any in-progress turn for this context
             const abortController = new AbortController();
             this.sessionManager.trackExecution(taskId, contextId, abortController);
             const turnFn = async () => {
+                // ④ v0.3.1: `timeouts.prompt` was declared but never used, so a hung
+                // backend left the task `working` forever. Bound the turn so it always
+                // reaches a detectable terminal. A value <= 0 disables the bound
+                // (mirrors the claude executor).
+                const promptTimeout = this.config.timeouts?.prompt ?? 600_000;
+                let timedOut = false;
+                let timeoutTimer = null;
+                // Rejects on timeout and aborts the backend stream. Declared outside the
+                // try so the `finally` can clear the timer.
+                const timeoutError = new Promise((_, reject) => {
+                    if (typeof promptTimeout === "number" && promptTimeout > 0) {
+                        timeoutTimer = setTimeout(() => {
+                            // Reject before aborting so the timeout (not the resulting
+                            // AbortError) wins the race and is classified as a failure.
+                            timedOut = true;
+                            reject(new Error(`Prompt timeout after ${promptTimeout}ms`));
+                            abortController.abort();
+                        }, promptTimeout);
+                        if (typeof timeoutTimer.unref === "function")
+                            timeoutTimer.unref();
+                    }
+                });
                 try {
                     publishStatus(bus, taskId, contextId, "working", "Processing request...");
                     let finalText = "";
@@ -123,15 +158,39 @@ export class CodexExecutor {
                     const agentMessageText = new Map();
                     const streamArtifactId = `response-${taskId}`;
                     let streamArtifactStarted = false;
-                    const { events } = await session.thread.runStreamed(promptText, {
-                        signal: abortController.signal,
-                    });
-                    for await (const event of events) {
+                    // v0.3.1: the @openai/codex-sdk ThreadOptions exposes no instructions
+                    // channel, so the declared `codex.developerInstructions` is wired here
+                    // by prepending it to every turn's prompt as developer context.
+                    const developerInstructions = this.config.codex.developerInstructions;
+                    const effectivePrompt = developerInstructions
+                        ? `${developerInstructions}\n\n---\n${promptText}`
+                        : promptText;
+                    const { events } = await Promise.race([
+                        session.thread.runStreamed(effectivePrompt, {
+                            signal: abortController.signal,
+                        }),
+                        timeoutError,
+                    ]);
+                    // Race every stream read against the timeout so the `for await` always
+                    // settles even if a hung backend ignores the abort signal.
+                    const guardedEvents = {
+                        async *[Symbol.asyncIterator]() {
+                            while (true) {
+                                const next = await Promise.race([events.next(), timeoutError]);
+                                if (next.done)
+                                    return;
+                                yield next.value;
+                            }
+                        },
+                    };
+                    for await (const event of guardedEvents) {
                         // Capture thread_id from first event
                         if (event.type === "thread.started" && session.threadId === null) {
                             const tid = mapper.handleEvent(event);
-                            if (tid)
+                            if (tid) {
                                 session.threadId = tid;
+                                this.sessionManager.persist();
+                            }
                             continue;
                         }
                         // Accumulate agent_message text across item.updated snapshots
@@ -155,6 +214,16 @@ export class CodexExecutor {
                         }
                         mapper.handleEvent(event);
                     }
+                    // ④: a timer abort can end the stream cleanly (no throw), so the
+                    // timeout must be reported here too — never as `completed`.
+                    if (timedOut) {
+                        const timeoutMsg = `Prompt timeout after ${promptTimeout}ms`;
+                        log.error("Task execution timed out", { taskId });
+                        publishStatus(bus, taskId, contextId, "failed", timeoutMsg, true);
+                        __a2aTaskLog.end(taskId, contextId, "failed", 0, __taskT0, timeoutMsg);
+                        bus.finished();
+                        return;
+                    }
                     // Publish artifact
                     if (this.config.features.streamArtifactChunks && streamArtifactStarted) {
                         publishLastChunkMarker(bus, taskId, contextId, streamArtifactId, finalText);
@@ -162,24 +231,40 @@ export class CodexExecutor {
                     else {
                         publishFinalArtifact(bus, taskId, contextId, finalText);
                     }
-                    publishStatus(bus, taskId, contextId, "completed", undefined, true);
+                    // ② v0.3.1: resolve the worker's declared `a2a-outcome` marker.
+                    const outcome = resolveOutcome(finalText);
+                    publishStatus(bus, taskId, contextId, outcome.state, outcome.message, true);
+                    __a2aTaskLog.end(taskId, contextId, outcome.state, finalText.length, __taskT0);
                     bus.finished();
                 }
                 catch (err) {
                     const isAbort = err instanceof Error &&
                         (err.name === "AbortError" || err.message.includes("abort") || err.message.includes("canceled"));
-                    if (isAbort) {
+                    if (isAbort && timedOut) {
+                        // ④ v0.3.1: our own timer aborted the turn — report a timeout
+                        // failure rather than silently swallowing it as a user cancel.
+                        const timeoutMsg = `Prompt timeout after ${promptTimeout}ms`;
+                        log.error("Task execution timed out", { taskId });
+                        publishStatus(bus, taskId, contextId, "failed", timeoutMsg, true);
+                        __a2aTaskLog.end(taskId, contextId, "failed", 0, __taskT0, timeoutMsg);
+                        bus.finished();
+                    }
+                    else if (isAbort) {
                         log.info("Task execution aborted", { taskId });
+                        __a2aTaskLog.end(taskId, contextId, "canceled", 0, __taskT0);
                         // cancelTask already published the canceled status
                     }
                     else {
                         const msg = sanitizeMessage(err instanceof Error ? err.message : String(err));
                         log.error("Task execution failed", { taskId, error: msg });
                         publishStatus(bus, taskId, contextId, "failed", msg, true);
+                        __a2aTaskLog.end(taskId, contextId, "failed", 0, __taskT0, msg);
                         bus.finished();
                     }
                 }
                 finally {
+                    if (timeoutTimer)
+                        clearTimeout(timeoutTimer);
                     this.sessionManager?.untrackExecution(taskId);
                 }
             };
@@ -193,6 +278,7 @@ export class CodexExecutor {
             const msg = sanitizeMessage(outerErr instanceof Error ? outerErr.message : String(outerErr));
             log.error("Executor outer error", { taskId, error: msg });
             publishStatus(bus, taskId, contextId, "failed", msg, true);
+            if (__taskT0 !== null) __a2aTaskLog.end(taskId, contextId, "failed", 0, __taskT0, msg);
             bus.finished();
             this.sessionManager?.untrackExecution(taskId);
         }
diff --git a/node_modules/a2a-codex/dist/codex/session-manager.js b/node_modules/a2a-codex/dist/codex/session-manager.js
index 3cf12dc..c0f92b3 100644
--- a/node_modules/a2a-codex/dist/codex/session-manager.js
+++ b/node_modules/a2a-codex/dist/codex/session-manager.js
@@ -6,6 +6,8 @@
  * Tracks active executions for cancellation support.
  */
 import { logger } from "../utils/logger.js";
+import { mkdirSync, readFileSync, writeFileSync } from "node:fs";
+import { dirname, join } from "node:path";
 const log = logger.child("session-manager");
 // ─── SessionManager ───────────────────────────────────────────────────────────
 export class SessionManager {
@@ -16,9 +18,61 @@ export class SessionManager {
     /** taskId → active execution */
     activeExecutions = new Map();
     cleanupTimer = null;
+    persistPath;
     constructor(client, config) {
         this.client = client;
         this.config = config;
+        this.persistPath = join(this.config.codex?.workingDirectory ?? process.cwd(), ".a2a", "sessions.json");
+        this.loadPersisted();
+    }
+    /**
+     * Load the persisted contextId → threadId map, resuming each Codex thread.
+     * Entries outside TTL or failing to resume are skipped; this never throws.
+     */
+    loadPersisted() {
+        try {
+            const data = JSON.parse(readFileSync(this.persistPath, "utf8"));
+            const ttl = this.config.session.ttl ?? 3_600_000;
+            const now = Date.now();
+            for (const [contextId, entry] of Object.entries(data)) {
+                if (!entry || typeof entry.threadId !== "string")
+                    continue;
+                const lastAccessedAt = entry.lastAccessedAt ?? entry.createdAt ?? 0;
+                if (now - lastAccessedAt > ttl) {
+                    log.debug("Skipping expired persisted session", { contextId, threadId: entry.threadId });
+                    continue;
+                }
+                try {
+                    const thread = this.client.resumeThread(entry.threadId, this.buildThreadOptions());
+                    this.sessions.set(contextId, {
+                        contextId,
+                        threadId: entry.threadId,
+                        thread,
+                        createdAt: entry.createdAt ?? entry.lastAccessedAt ?? Date.now(),
+                        lastAccessedAt: entry.lastAccessedAt ?? Date.now(),
+                        executionQueue: Promise.resolve(),
+                    });
+                }
+                catch (err) {
+                    log.warn("Failed to resume persisted Codex thread", { contextId, threadId: entry.threadId, error: String(err) });
+                }
+            }
+        }
+        catch {
+            // No persisted map yet (or unreadable); start empty.
+        }
+    }
+    /** Persist the contextId → threadId map to disk. */
+    persist() {
+        try {
+            mkdirSync(dirname(this.persistPath), { recursive: true });
+            writeFileSync(this.persistPath, JSON.stringify(Object.fromEntries([...this.sessions.entries()]
+                .filter(([, s]) => typeof s.threadId === "string" && s.threadId)
+                .map(([ctx, s]) => [ctx, { threadId: s.threadId, createdAt: s.createdAt, lastAccessedAt: s.lastAccessedAt }]))), "utf8");
+        }
+        catch (err) {
+            log.warn("Failed to persist sessions", { error: String(err) });
+        }
     }
     /**
      * Get an existing session for the contextId, or create a new one using the
@@ -39,6 +93,7 @@ export class SessionManager {
                 }
                 log.info("Session TTL expired, starting new thread", { contextId, age });
                 this.sessions.delete(contextId);
+                this.persist();
             }
         }
         const thread = threadFactory();
@@ -52,6 +107,7 @@ export class SessionManager {
         };
         if (contextId) {
             this.sessions.set(contextId, session);
+            this.persist();
         }
         log.info("Started new Codex thread", { contextId });
         return session;
@@ -83,6 +139,7 @@ export class SessionManager {
             return;
         this.cleanupTimer = setInterval(() => {
             const now = Date.now();
+            let deleted = 0;
             for (const [contextId, session] of this.sessions.entries()) {
                 if (now - session.lastAccessedAt > ttl) {
                     // Do not remove if there is an active execution for this context
@@ -93,8 +150,12 @@ export class SessionManager {
                     }
                     log.info("TTL cleanup: removing stale session", { contextId, threadId: session.threadId });
                     this.sessions.delete(contextId);
+                    deleted++;
                 }
             }
+            if (deleted > 0) {
+                this.persist();
+            }
         }, interval);
     }
     /** Stop the cleanup timer. */
