diff --git a/node_modules/a2a-opencode/dist/opencode/executor.js b/node_modules/a2a-opencode/dist/opencode/executor.js
index 1bf46bf..d3399cc 100644
--- a/node_modules/a2a-opencode/dist/opencode/executor.js
+++ b/node_modules/a2a-opencode/dist/opencode/executor.js
@@ -20,6 +20,17 @@ import { publishStatus, publishFinalArtifact, publishStreamingChunk, publishLast
 import { createDeferred, sleep } from "../utils/deferred.js";
 import { logger } from "../utils/logger.js";
 const log = logger.child("executor");
+// v0.1.2 统一任务执行日志（[a2a-task]）：直写 stdout，不受 logging.level 影响，供 Launcher 观测开始/结束/耗时。
+const __a2aTaskLog = {
+    start(taskId, contextId, promptLen) {
+        console.log(`[a2a-task] start taskId=${taskId} contextId=${contextId} promptLen=${promptLen}`);
+    },
+    end(taskId, contextId, state, responseChars, startedAt, error) {
+        const durationMs = Date.now() - startedAt;
+        const suffix = error ? ` error=${JSON.stringify(String(error).slice(0, 300))}` : "";
+        console.log(`[a2a-task] end taskId=${taskId} contextId=${contextId} state=${state} responseChars=${responseChars} durationMs=${durationMs}${suffix}`);
+    },
+};
 // ─── Trace Helpers ──────────────────────────────────────────────────────────
 const MAX_DATA_SIZE = 100_000;
 const SENSITIVE_KEYS = new Set([
@@ -293,6 +304,7 @@ export class OpenCodeExecutor {
     // ── Execute ─────────────────────────────────────────────────────────────
     async execute(ctx, bus) {
         const { taskId, contextId, userMessage, task } = ctx;
+        let __taskT0 = null;
         await this.initialize();
         let stream = null;
         // Extract trace context propagated by the orchestrator via A2A metadata
@@ -347,7 +359,8 @@ export class OpenCodeExecutor {
                 this.promptedSessions.add(sessionId);
             }
             const promptBody = this.buildPromptBody(promptText);
-            log.info("Sending prompt", { taskId, sessionId, len: promptText.length });
+            __taskT0 = Date.now();
+            __a2aTaskLog.start(taskId, contextId, promptText.length);
             // 5. SSE stream
             const dir = this.config.opencode.projectDirectory || undefined;
             stream = new EventStreamManager(this.client, {
@@ -517,6 +530,7 @@ export class OpenCodeExecutor {
                 publishFinalArtifact(bus, taskId, contextId, accumulatedText);
             }
             publishStatus(bus, taskId, contextId, "completed", undefined, true);
+            __a2aTaskLog.end(taskId, contextId, "completed", accumulatedText.length, __taskT0);
             bus.finished();
             log.info("Task completed", { taskId, len: accumulatedText.length });
         }
@@ -529,6 +543,7 @@ export class OpenCodeExecutor {
                 : `Error: ${msg}`;
             log.error("Execution failed", { taskId, error: msg });
             publishStatus(bus, taskId, contextId, "failed", userMsg, true);
+            if (__taskT0 !== null) __a2aTaskLog.end(taskId, contextId, "failed", 0, __taskT0, msg);
             bus.finished();
         }
         finally {
diff --git a/node_modules/a2a-opencode/dist/opencode/session-manager.js b/node_modules/a2a-opencode/dist/opencode/session-manager.js
index bd06bf0..e6574d5 100644
--- a/node_modules/a2a-opencode/dist/opencode/session-manager.js
+++ b/node_modules/a2a-opencode/dist/opencode/session-manager.js
@@ -5,6 +5,8 @@
  * Extracted from executor to keep each module focused.
  */
 import { logger } from "../utils/logger.js";
+import { mkdirSync, readFileSync, writeFileSync } from "node:fs";
+import { dirname, join } from "node:path";
 const log = logger.child("sessions");
 // ─── Auto-Allow Permissions ─────────────────────────────────────────────────
 const AUTO_ALLOW_PERMISSIONS = [
@@ -24,6 +26,7 @@ export class SessionManager {
     sessionCfg;
     autoApprove;
     directory;
+    persistPath;
     contextMap = new Map();
     taskMap = new Map(); // taskId → sessionId
     taskContexts = new Map(); // taskId → contextId
@@ -33,6 +36,35 @@ export class SessionManager {
         this.sessionCfg = sessionCfg;
         this.autoApprove = features.autoApprovePermissions;
         this.directory = directory;
+        this.persistPath = join(this.directory ?? process.cwd(), ".a2a", "sessions.json");
+        this.loadPersisted();
+    }
+    /** Load persisted contextId → sessionId map, skipping entries older than TTL. */
+    loadPersisted() {
+        try {
+            const data = JSON.parse(readFileSync(this.persistPath, "utf8"));
+            const now = Date.now();
+            for (const [contextId, entry] of Object.entries(data)) {
+                if (entry && typeof entry.sessionId === "string" && now - entry.lastUsed <= this.sessionCfg.ttl) {
+                    this.contextMap.set(contextId, { sessionId: entry.sessionId, lastUsed: entry.lastUsed });
+                }
+            }
+        }
+        catch {
+            // No persisted map yet (or unreadable); start empty.
+        }
+    }
+    /** Persist the contextId → sessionId map to disk. */
+    persist() {
+        if (!this.sessionCfg.reuseByContext)
+            return;
+        try {
+            mkdirSync(dirname(this.persistPath), { recursive: true });
+            writeFileSync(this.persistPath, JSON.stringify(Object.fromEntries(this.contextMap)), "utf8");
+        }
+        catch (err) {
+            log.warn("Failed to persist sessions", { error: String(err) });
+        }
     }
     /** Start periodic session cleanup. */
     startCleanup() {
@@ -47,8 +79,10 @@ export class SessionManager {
                     cleaned++;
                 }
             }
-            if (cleaned > 0)
+            if (cleaned > 0) {
+                this.persist();
                 log.info("Cleaned expired sessions", { count: cleaned });
+            }
         }, this.sessionCfg.cleanupInterval);
         if (this.cleanupTimer.unref)
             this.cleanupTimer.unref();
@@ -71,10 +105,12 @@ export class SessionManager {
                 entry.lastUsed = Date.now();
                 try {
                     await this.client.sessionGet(entry.sessionId, this.directory || undefined);
+                    this.persist();
                     return entry.sessionId;
                 }
                 catch {
                     this.contextMap.delete(contextId);
+                    this.persist();
                 }
             }
         }
@@ -85,6 +121,7 @@ export class SessionManager {
         });
         if (this.sessionCfg.reuseByContext) {
             this.contextMap.set(contextId, { sessionId: session.id, lastUsed: Date.now() });
+            this.persist();
         }
         log.info("Session ready", { sessionId: session.id, contextId });
         return session.id;
