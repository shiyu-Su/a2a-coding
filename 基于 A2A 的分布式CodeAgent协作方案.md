# 基于 A2A 的分布式 Code Agent 协作方案

## 一、背景与目标

当前开发环境存在物理或逻辑隔离：前端服务运行在 Windows，后端服务运行在 Mac。希望在统一入口中完成需求讨论，并在需要时触发远端或另一环境的 Agent 执行验证、开发等任务。

核心目标：

- 不改造现有 Agent 内部，只在其 CLI/headless 能力之上包一层。
- 复用现有主流 Code Agent，如 Claude Code、OpenCode、Codex 等。
- 用 A2A 连接分布式 Agent，实现任务委派与结果回传。
- 统一讨论入口在 orch Agent，前后端 Agent 按需执行。
- 方案不仅适用于跨机器，也适用于同机多项目、前后端分离、多角色协作。

## 二、核心原则

1. **Agent 只作为执行单元**：不碰 Agent 内部逻辑，只调用其 CLI/headless 模式。
2. **A2A 负责 Agent 间通信**：用于任务委派、状态查询、结果回传。
3. **MCP 可选，不作为跨 Agent 通信主干**：MCP 更适合工具调用；本方案跨 Agent 协作以 A2A 为主。
4. **同机与跨机一致**：架构不依赖“必须跨机器”，同机同样成立。
5. **前后端分离是首个场景，不是唯一场景**：可扩展到审查 Agent、测试 Agent、多项目 Agent 等。

## 三、总体架构

分为三层：

- **会话层**：orch Agent，负责与用户对话、拆解需求、决定何时派发任务。
- **网关层**：把前后端 Agent 包装成 A2A Server，同时让 orch 具备 A2A Client 能力。
- **执行层**：前后端 Agent 在各自 workspace 中调用内置工具或 CLI 执行任务。

整体链路：

用户 ↔ orch Agent ↔ A2A 网关 ↔ 前端/后端 Agent ↔ 各自环境执行

## 四、角色分工

| 角色 | 位置 | 职责 |
|---|---|---|
| orch Agent | Windows 或任意位置 | 对话、需求拆解、任务编排、结果汇总 |
| 前端 Agent | Windows | 前端项目开发与验证 |
| 后端 Agent | Mac | 后端项目开发与验证 |
| 可选扩展 Agent | 任意位置 | 审查、测试、部署等专项职责 |

三个 Agent 是最小可行组合：一个编排，两个执行。

## 五、协议与通信

- **上行**：orch 通过 A2A 协议发现并调用前后端 Agent。
- **下行**：A2A 网关将任务转换为对应 Agent 的 CLI/headless 调用。
- **多轮对话**：对外用 `contextId` 表示逻辑会话，对内映射到底层 CLI 的 Session ID。
- **任务状态**：支持 working、completed、failed、input-required 等状态，便于处理澄清和人工介入。

## 六、关键机制

### 1. 多轮对话

CLI 非交互模式并非不能多轮。网关层维护 `contextId` 与底层 Agent Session 的映射，每次调用恢复对应会话，即可保持上下文连续。

### 2. 人工介入

非交互模式下，权限应在调用前预先设计：

- 可自动批准低风险操作；
- 可限定允许的工具范围；
- 越界操作直接失败并回传；
- 需要澄清时，以 `input-required` 状态回传，由 orch 决定如何继续。

### 3. 隔离与并发

前后端 Agent 绑定不同 workspace，按项目目录隔离。不同 Agent 可并行执行，互不干扰；同一 Agent 内部会话独立管理。

### 4. 会话持久化

网关持久化 Session 绑定，重启后可恢复，避免每次重新初始化上下文。

## 七、组件选型与复用

- **前后端 Agent 包装**：可使用 `@axiom-lattice/cli-a2a` 将 OpenCode、Codex、Claude Code 等包装为 A2A Server。
- **orch 的 A2A Client 能力**：主流 Agent 原生支持 A2A 的很少，需要额外桥接。`a2a-bridge` 对主流 Agent 适配较广；也可用 MCP 桥接方式让 orch 获得调用 A2A 端点的工具。
- **原则**：核心协议处理、会话映射、CLI 适配优先复用现成项目，自己只做配置、适配和场景整合。

## 八、部署形态

方案支持多种部署形态：

- **跨机跨 OS**：Windows orch + Mac 前后端 Agent，原始场景。
- **跨机同 OS**：两台 Mac 或两台 Windows 之间的 Agent 协作。
- **同机多项目**：同一台机器上前后端 Agent 分目录、分 workspace 协作。
- **多角色扩展**：开发、审查、测试 Agent 分布在同机或跨机。

跨机时需注意网络可达性、认证、地址广播与安全边界；同机时链路简化为本地调用。

## 九、工作流概览

1. 用户在 orch 中讨论需求。
2. orch 判断是否需要前端或后端执行。
3. orch 通过 A2A 向对应 Agent 派发任务。
4. 目标 Agent 在本地 workspace 执行，返回结果或澄清请求。
5. orch 汇总结果，继续与用户对话，或发起下一轮任务。

## 十、创新性与定位

该方案不是理论层面的开创，但在工程和产品层面具备创新性：

- 将分布式异构 Code Agent 通过 A2A 网关统一协作；
- 以“讨论在本地、执行在远端/异环境”为产品体验；
- 首个场景是前后端分离，但可推广到同机多 Agent、多角色协作；
- 复用现有 Agent 与现成网关项目，不从头造核心协议。

## 十一、风险与待验证点

- 主流 Agent 原生 A2A 支持有限，依赖桥接或网关。
- 非交互模式的权限策略需要提前设计。
- 跨机认证与安全需额外补足。
- 具体 Agent 的 CLI/headless 兼容性需逐一验证，例如 ZCode 等需确认其是否可被桥接。

## 结论

整体方案可行：以 orch 为统一入口，前后端 Agent 作为执行节点，通过 A2A 网关连接，向下复用各 Agent 的 CLI/headless 能力，向上提供标准 A2A 通信。它能实现 Windows 讨论、Mac 执行，也能扩展到同机多项目、前后端分离和多角色协作场景，且不需要修改 Agent 内部，只需在外部完成配置、桥接和场景整合。