# {Project Name} — {Project Description}

> 花括号 `{...}` 部分需替换为实际项目内容。
> 使用前请全局搜索 `TODO` 和 `{` 定位所有需要自定义的位置。

---

## Project Context

{TODO: 项目一句话描述，目标用户，核心功能}

数据来源：{TODO: 外部数据源或服务}

## 技术栈

{TODO: 按实际技术栈填写}

| 层 | 技术 |
|----|------|
| {前端框架} | {具体技术} |
| {后端} | {具体技术} |
| {数据库} | {具体技术} |
| {HTTP 请求} | {具体技术} |
| {NLU 解析} | {具体技术} |
| {缓存} | {具体技术} |

## 架构约束（设计/实现必须遵循）

本项目基于 **{TODO: 框架名}** 框架，以下约束在方案设计和代码实现中**不得违反**。

### {TODO: 前端层约束，如适用}

| 约束 | 说明 | 违反示例 |
|------|------|----------|
| **{约束名}** | {说明} | {违反示例} |

### {TODO: 后端层约束}

| 约束 | 说明 | 违反示例 |
|------|------|----------|
| **{约束名}** | {说明} | {违反示例} |

### {TODO: 数据库约束}

| 约束 | 说明 | 违反示例 |
|------|------|----------|
| **{约束名}** | {说明} | {违反示例} |

### {TODO: 业务流程约束}

| 约束 | 说明 | 违反示例 |
|------|------|----------|
| **{约束名}** | {说明} | {违反示例} |

---

## 核心规范（必须遵守）

### 绝对规则

- **不得擅自改代码** — 用户没有明确说「改/实现/加/修」等实现动词时，禁止编辑任何代码文件。用户提问、分析、反馈问题时，只回答/分析，不动代码。
- **用户测试反馈后必须等待确认** — 测试发现问题后，只分析根因、汇报方案，等待用户确认「改」之后再动手修改。不得在汇报后自行继续修改或推进。
- **规范优于经验** — 任何与规范冲突的情况，以本文件书面规则为准，不以经验或常识判断替代。

### 需求流程（Requirement Flow）

每个需求从提出到验收通过的完整链路。多个需求可独立走此流程，累计构成本版本内容。用户提出改动需求时，必须从步骤 0 开始，禁止直接实现。

```
0. 需求访谈 → 输出《需求确认单》
   ↓ 【用户确认需求内容】
1. 需求评估 + 输出改造文档（评估结论内嵌）→ doc/{版本}/
   ↓ 【用户确认改造文档】
2. 需求实现（拆子任务 → 子 agent 并行执行）
   ↓
3. 用户测试 + 反馈
   │
   ├─ 发现影响验收标准的问题 → 在当前需求中增补（改造文档 +1 版）
   ├─ 发现影响使用/结构的新需求 → 用户确认当前需求状态 → 记录版本待办 → 回到步骤 0
   ├─ 发现非影响性改进点 → 登记到 planlog（一句话，不编号）
   └─ 用户确认「通过」 → 需求完成
```

> **步骤 1（用户确认改造文档）之前禁止开始实现。改造文档输出后不能直接开始实现，必须等用户确认。**
> **⚠️ 步骤 3 是需求流程的终点。用户确认「通过」后该需求结束，禁止自行进入发版操作（回归测试、CHANGELOG 更新、git commit、tag 等）。**
> **⚠️ 步骤 2 产出的 plan 严禁包含发版相关步骤。发版操作只能由用户主动要求后通过发版流程执行。**

#### 每步推进的强制条件

需求从 Step 0 → Step 1、Step 1 → Step 2、Step 2 → Step 3 推进之前，必须同时满足以下两项，否则不得进入下一步：

1. **文档已落地**：当前步骤对应文档已写入 `doc/{版本}/` 目录。
   - Step 0 输出：`{topic}-需求确认单.md`
   - Step 1 输出：`{topic}-改造文档.md`
   - Step 2/3 如有修订：同步更新改造文档（修订历史 +1 版）
2. **planlog 已同步**：`planlog.md` 顶部版本待办中，该需求的「当前步骤」标记已更新到即将进入的步骤。

### 发版流程（Release Flow）

当用户决定发布当前版本时执行。发版流程独立于需求流程，不对应某个具体需求。

```
0. 检查版本待办（planlog.md 顶部）— 确认当前版本所有需求均为 ✅ 已完成
1. 发版前文档审核：
   a. 审核 doc/{版本}/ 文档，确认与实际代码一致（已在需求流程中同步完成，此处仅验证）
   b. 更新 CHANGELOG.md，记录本次版本所有变更
   c. 更新 README.md（如有架构、API 或流程变化）
   d. 同步 planlog.md — 将已完成的改进项标记为完成/移除，新增本轮发现的问题
   e. 检查回归测试脚本，确认已有用例覆盖当前版本所有新增模块和检测项，
      如有新模块/新文件需补充对应检测用例
2. {回归测试命令}
3. 检查 reports/v{版本号}/regression-report.json 确认全部通过，用于版本记录
4. git add + commit
5. git tag v0.x.x
6. git push && git push --tags（同步到远程仓库）
7. 发布到目标环境
```

#### 多轮对话流程纪律（AI 遵守）

AI 在跨轮对话中执行流程时，必须遵守以下纪律：

```
1. 每轮只进一个步骤
   用户的"确认/同意"只针对当前步骤，不隐含对后续步骤的授权。
   → 用户说"好"→ 只代表当前步骤通过，不能自动进入下一步。

2. 不得跨步输出
   禁止在同一轮回复中同时完成多个步骤。
   → 举例：不能在同一轮既输出需求确认单（步骤0）又输出改造文档（步骤1）。
   → 必须等用户对当前步骤的输出做出反应后，再进入下一步。

3. TODO continuation 不推进流程
   系统触发继续执行时，只完成当前已授权步骤内的剩余工作。
   → 步骤 2 的实现中途被系统 continuation 触发 → 继续实现，不自行进入步骤 3。
   → 步骤 3 被 continuation 触发 → 等待用户反馈结果或决策，不自行进入发版流程。

4. 每轮重置意图判断
   用户新的一轮输入，只根据本轮内容判断意图。
   → 用户说"改一下"→ 重新判断是哪个步骤，不是默认进入"实现"阶段（步骤 2）。
   → 用户说"看看效果"→ 只做展示/说明，不是默认进入"测试"阶段（步骤 3）。
   → 用户说"发版"→ 才进入发版流程。
```

**各阶段说明：**

- **需求访谈（步骤 0）**：
  - 目标：在进入技术方案之前，先确认「问题定义」是否正确
  - 输出：《需求确认单》。**强制要求：必须写入 `doc/{版本}/{topic}-需求确认单.md` 文件，不得仅在对话中输出或口头确认。** 使用 `doc/_templates/需求确认单.md` 模板。
  - 输出后先让用户确认：「我理解你要解决的是不是这个问题？」—— 确认的是问题，不是方案
  - 只有需求确认单经用户确认后，才进入下一步

- **需求评估 + 输出改造文档（步骤 1）**：
  - 需求评估：检查是否与架构冲突、是否有依赖、是否有更简方案、是否与现有需求冲突。评估后需引用 REQ-ID。结论：合理 / 需调整 / 不合理
  - **评估合理后 → 必须输出改造文档到 `doc/{版本}/{topic}-改造文档.md` 文件，不得仅在对话中输出。** 使用【改造文档模板】（见下方）。
  - 评估结论内嵌在改造文档的「背景」或「方案设计」章节中
  - 必须等待用户确认改造文档后，才进入实现阶段

- **需求实现（步骤 2）**：按改造文档实现，拆为独立子任务并行执行
  - **实现中如偏离改造文档**：如果实现方式、接口、行为与改造文档不一致，必须同步更新改造文档（修订历史 + 1 版），不得等实现完成后再补
  - **实现中发现 scope 外的问题**：实现需求 A 过程中，如果发现新的 bug、改进点、或与需求 A 无关的功能缺失，按以下规则处理：
    ```
    发现新问题
      → 判断是否影响需求A【验收标准】的达成
      → 是 → 属于需求A的增补
          → 同步更新改造文档：
            - 验收标准中补充新场景
            - In Scope 中补充涉及文件
            - 修订历史 +1 版
          → 继续实现，不中断
      → 否 → 超出需求A范围
          → 在 planlog.md 中登记（一句话，不编号）
          → 继续需求A，不中断
    ```
  - **禁止在需求 A 的实现中顺手修需求 B 的问题**。例外：纯格式问题可以直接修并同步说明；≤3 行/≤15min 的低成本修复可以在 CHANGELOG 中备注后直接修，不违反此规则。

- **用户测试（步骤 3）**：实现完成后仅向用户展示变更摘要和测试结果，等待用户反馈。
  - 禁止执行任何回归测试、CHANGELOG 更新、git 操作
  - 用户根据测试结果做决策：
    - **发现影响验收标准的问题** → 在当前需求中增补（改造文档 +1 版）
    - **发现影响使用或结构的新需求** → 用户先确认当前需求的完成状态 → 记录版本待办 → 从步骤 0 开始新需求
    - **发现非影响性改进点** → 一句话登记到 planlog，不展开
    - **用户确认「通过」** → 同步更新改造文档修订历史（标记 final 版本）→ 需求完成，等待发版

- **发版流程**：当用户主动要求发版时执行。不是需求流程的延续，而是独立操作。
  - 发版前完成文档对齐（doc、CHANGELOG、README、planlog）
  - 运行回归测试并确认全部通过
  - git commit + tag
  - 发布到目标环境

### 文档命名规范

每个需求在 Step 0 和 Step 1 必须产出两个文件，命名统一为：

| 文档 | 文件路径 | 命名格式 |
|------|----------|----------|
| 需求确认单 | `doc/{版本}/{topic}-需求确认单.md` | `{topic}-需求确认单.md` |
| 改造文档 | `doc/{版本}/{topic}-改造文档.md` | `{topic}-改造文档.md` |

- `{topic}` 为该需求的简短中文摘要（5-10 字），同一个需求的两个文件使用同一 `{topic}`，便于配对和追溯。
- 禁止单独使用 `需求确认单.md`、`改造文档.md` 等无 topic 前缀的文件名。
- 禁止混用 `方案`、`方案设计`、`实现方案`、`修复`、`优化`、`分析` 等后缀，统一使用 `改造文档` 作为后缀。

---

#### 【改造文档模板】

每个版本目录下的方案文档必须包含以下章节：

```
# vX.Y.Z 标题

## 0. 需求信息
- REQ-ID: REQ-YYYY-MM-XX

改造文档中引用需求时，REQ-ID 格式为 `REQ-v0.x.x-YYYY-MM-DD-XX`（如 `REQ-v0.9.1-2026-07-21-01`），其中 XX 为该版本当日内的序号。
- 需求来源: 用户反馈 / 自发现 / 技术债 / 产品规划
- 原始需求: 用户原始描述或问题现象
- 优先级: P0 / P1 / P2

## 1. 背景
为什么需要这个改动？当前存在的问题或缺失的能力。

## 2. 方案设计
具体实现方案，包含数据流、关键代码逻辑、接口变更等。

## 3. 变更范围

### 3.1 In Scope（涉及文件 + 改动量评估）
| 文件 | 改动类型 | 改动量 | 说明 |
|------|----------|--------|------|

### 3.2 Out of Scope（显式声明本次不做的事）
- ...（明确列出，防止 scope creep）
- 实现过程中发现的新问题（非本次 In Scope）→ 登记到 planlog.md，不在此次实现

## 4. 验收标准

### 4.1 实现级（单元场景）
逐个功能点的验证，输入→预期输出。

### 4.2 场景级（用户视角）
站在用户角度验证：用户看到/体验到什么。

### 4.3 回归级（不破坏现有功能）
确认没有破坏已有功能的具体验证点。

## 5. 修订历史
| 版本 | 日期 | 修订人 | 说明 |
|------|------|--------|------|
| v1 | YYYY-MM-DD | - | 初稿 |
| v2 | YYYY-MM-DD | - | 对齐实际实现（实现完成后更新） |
| v3 | YYYY-MM-DD | - | 二次修改：xxx（用户测试发现问题后更新） |
```

### 开发中的文档同步

```
代码改动后必须同步更新文档，不允许出现「代码改了，文档还停在原版」的情况。
```

- 实现完成后立即检查 `doc/{版本}/` 是否需要更新
- 发版前必须做一次文档对齐，确保文档与代码一致
- 架构图、数据流、字段说明必须与实际代码对应

**文档更新时机（必须遵循）**：

| 时机 | 动作 | 修订历史标记 |
|------|------|-------------|
| 改造文档初稿完成 | 写入 `doc/{版本}/` | v1 |
| 实现中偏离原方案（接口/行为/范围变化） | 立即同步更新 | +1 版（v2、v3…） |
| 二次修改（用户测试发现问题后） | 修改代码后同步更新 | +1 版 |
| 用户确认测试通过后 | 最终审核对齐 | 标记 final 版本 |

### 测试中的问题处理

回归测试或日常测试中发现的问题，**不得擅自修复**，必须走以下流程：

```
发现缺陷或异常
  → 记录问题现象
  → 分析根因 + 修复方案
  → 向用户汇报
  → 等待用户确认
  → 确认后再修改
  → 修改后重新测试
```

**例外**：纯格式问题（缩进、空格、注释）或 CI 配置修复，可以直接修并同步说明。

### 编码规范

{TODO: 按项目实际情况填写，以下为示例}

1. **字段命名** — 全部 {camelCase / snake_case / PascalCase}
2. **时区** — 所有时间戳使用 {某时区}，格式示例
3. **错误处理** — 所有外部请求失败不抛异常，降级返回空结果
4. {其他自定义规范}

### API 规范

以下规约 API 的**入参与出参**，而非罗列具体端点（具体端点应在 README 中维护）。

#### 入参规范

| 规范 | 说明 | 违反示例 |
|------|------|----------|
| **字段命名** | 入参字段名统一使用 camelCase（JSON body）或 kebab-case（URL 参数） | `user_id`、`job_title` |
| **必填校验** | 必填参数缺失时返回统一错误格式，不抛未捕获异常 | 不校验直接 `req.body.userId` 导致 undefined 异常 |
| **类型明确** | 数值、布尔值使用原生类型而非字符串 | `"true"` 代替 `true`、`"123"` 代替 `123` |
| **数组入参** | 明确约定数组格式（逗号分隔 / JSON 数组） | 接口混用 `a,b,c` 和 `["a","b","c"]` |

#### 出参规范

| 规范 | 说明 | 违反示例 |
|------|------|----------|
| **统一返回格式** | 所有成功/失败响应使用统一外壳：`{ result, data, error }` | 成功返回 `{ success: true, data }`，失败返回 `{ error: msg }` |
| **字段命名** | 出参字段统一 camelCase | `star_stories`、`company_name` |
| **空值处理** | 无数据时返回 `null` 或空数组 `[]`，不返回 `undefined` 或省略字段 | 无数据时字段缺失 |
| **错误信息** | 错误时 `error` 字段为人类可读字符串，`data` 固定为 `null` | 错误时 `data` 还携带部分数据 |
| **分页格式** | 列表接口统一 `{ list: [], total, page, pageSize }` | 各接口分页格式不一致 |

#### 通用规范

| 规范 | 说明 |
|------|------|
| **HTTP 方法** | GET 查 / POST 增 / PUT 全量改 / PATCH 部分改 / DELETE 删 |
| **状态码** | 200 成功 / 400 参数错误 / 401 未认证 / 403 无权限 / 404 不存在 / 500 服务端错误 |
| **认证方式** | 统一认证头格式，如 `Authorization: Bearer <token>` |
| **版本前缀** | 同一大版本下不 breaking change 时不加 path 版本号；breaking change 时前缀 `/v2/` |

### 版本管理

#### 版本号规则

```
v<major>.<minor>.<patch>

v0.x.x  ← 当前阶段（持续迭代）
v1.0.0  ← 功能完备后正式定版
```

#### 版本待办

版本待办记录当前版本所有需求及其状态，维护在 `planlog.md` 顶部。发版前必须确认所有需求均为 ✅ 已完成。

| 状态 | 含义 |
|------|------|
| ✅ 已完成 | 用户测试通过，等待发版 |
| 🔄 进行中 | 当前正在处理 |
| 📋 待开始 | 已确认但尚未实现 |
| ⏸ 已暂停 | 被其他需求阻塞或推迟 |

**触发时机**：

1. **版本规划时** — 初始化版本待办，列出已确认的需求
2. **步骤 3 发现新需求时** — 先确认当前需求状态（通过/暂停），再追加新条目

**每次进入需求流程的下一步时，同步更新 planlog 中该需求的「当前步骤」标记**，避免上下文丢失和跳步。

**发版后** — 清空 planlog 顶部的版本待办（已完成项在 doc/ 和 CHANGELOG 中已有记录），底部已实现清单追加版本标记行。

#### 发版流程

参见上方【发版流程（Release Flow）】，版本发布严格执行该流程。

### 回归测试

发版前必须执行回归测试，且必须先更新回归测试用例，再运行。

**更新用例原则**：每个版本有新文件、新云函数、新模块时，必须在回归测试脚本中补充对应检测项（文件存在性、exports.main、依赖声明等）。

```
{TODO: 回归测试执行命令，如 cd .omo/skills/regression-test && node index.js}
```

运行后自动在 `reports/v{版本号}/regression-report.json` 生成结构化报告，包含版本号、时间戳、每条用例通过/失败详情。发版前检查此文件确保全部通过，也可用于 CI 消费。

**新增测试约定**：建议将回归测试拆分为模块化结构（`index.js + lib/ + tests/`），新增测试只需在 `tests/` 下新建 `test-xxx.js`，导出 `run()` 函数即可自动被发现和执行。详见项目的 skill 文档。

### 不提交的文件

`node_modules/`、`__pycache__/`、`*.pyc`、`.playwright-mcp/`、`.codegraph/`、`*.log`、`.env`、`.omo/run-continuation/`、`reports/`、`Thumbs.db`

---

## 附录一：需求确认单模板

（见独立文件 `doc/_templates/需求确认单.md`）

```
# 需求确认单

**REQ-ID**: REQ-YYYY-MM-XX
**来源**: 用户反馈 / 自发现 / 技术债 / 产品规划
**原始描述**:
> 用户的原始语句，一字不改

**要解决什么问题**:
- 当前行为: 
- 期望行为: 

**影响范围（粗判）**: {TODO: 涉及模块/层}

**成功标准（用户语言）**: 怎样算"做好了"

**参考/竞品**: （如有）

**优先级**: P0(阻塞发版) / P1(重要) / P2(锦上添花)
```

---

> **使用说明**
>
> 1. 将本文件中的 `{TODO: ...}` 全部替换为实际项目内容
> 2. 删除不需要的约束分类（如无数据库则删除数据库约束节）
> 3. 建议保留完整的「需求工作流」和「改造文档模板」，这是核心方法论
> 4. 随着项目演进，将踩过的坑沉淀为新约束追加到「架构约束」节
